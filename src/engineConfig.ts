/**Querry selector for the canvas element
*/
const canvasSelector = "#game";
const resizeViewport = true;
/**Target frames per second
*/
const fps = 60;

export {
    canvasSelector,    
    fps,
    resizeViewport
}